# Build artifacts
/target/
/dist/

# IDE & Editor
.idea/
.vscode/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Rust
*.pdb
**/*.rs.bk
# Note: Cargo.lock IS committed for binary crates (reproducible builds)

# Test artifacts
/test-output/

# Coverage reports
/target/llvm-cov/
lcov.info
*.profraw
*.profdata

# Environment & Secrets (never commit)
.env
.env.*
*.pem
*.key
secrets/

# Generated files (managed by calvin)
# These can be regenerated from .promptpack/ with `calvin deploy`
.claude/
.cursor/
.codex/
.agent/
.github/copilot-instructions.md
.github/instructions/
# Note: AGENTS.md is NOT ignored for this project (it's our dev guide)

# Lockfile tracks user-specific deployment state (should not be committed)
.promptpack/.calvin.lock

# Keep .promptpack/ (source of truth for prompts)
# !.promptpack/